org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Enables the experimental Gradle feature preview for compose
android.experimental.enableScreenshotTest=true

# Google Drive API Client ID (replace with your actual client ID from Google Cloud Console)
# Format: 123456789012-abcdefghijklmnopqrstuvwxyz123456.apps.googleusercontent.com
# To set up:
# 1. Go to Google Cloud Console
# 2. Create OAuth 2.0 credentials
# 3. Add your app's SHA-1 fingerprint
# 4. Replace the value below with your actual client ID
# Note: Keep this file in .gitignore or use environment variables for production
GOOGLE_DRIVE_CLIENT_ID=your_client_id_here
